export interface Image {
    id: string
    title: string
    description?: string
    filename: string
    hasImage?: boolean
    textContent?: string
    createdAt: string
    collectionId: string
}